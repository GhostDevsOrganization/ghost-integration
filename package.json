{
  "name": "ghost-integration-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "vercel-build": "npm run build"
  }
}
